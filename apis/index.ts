import { default as puzzles } from './puzzles';
import { default as friends } from './friends';
import { default as users } from './users';

const apis = {
  puzzles,
  friends,
  users,
};

export default apis;
